apiVersion: v1
kind: Service
metadata:
  name: svc-frontend
  namespace: sispardt
  labels:
    app: svc-frontend
spec:
  selector:
    app: svc-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
