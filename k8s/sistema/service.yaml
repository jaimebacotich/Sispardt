apiVersion: v1
kind: Service
metadata:
  name: svc-sistema
  namespace: sispardt
  labels:
    app: svc-sistema
spec:
  type: NodePort
  selector:
    app: svc-sistema
  ports:
    - name: http
      port: 8085
      targetPort: 8085
      nodePort: 30085
